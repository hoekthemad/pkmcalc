/**
 * Calculate
 * - happiness
 * - all prices
 * - shop prices
 * - income
 * - skill effects (from concentration + bonus)
 * - bonus relic levels
 * - atk dur
 * - en. atk dur
 * - atk power
 * - atk res
 * - insanity threshold
 * - greed (halp)
 */

const getHappiness = () => {
    let happiness = 1;
    return happiness;
}

const getAllPrice = () => {
    let allPrice = 1;
    return allPrice;
}

const getShopPrice = () => {
    let shopPrice = 1;
    return shopPrice;
}

const getIncome = () => {
    let income = 1;
    return income;
}

const getSkillEffect = () => {
    let skillEffect = 1;
    return skillEffect;
}

const getBonusRelicLevels = () => {
    let bonusRelicLevel = 1;
    return bonusRelicLevel > 1 ? bonusRelicLevel : 0;
}

const getAttackDuration = () => {
    let attackDuration = 1;
    return attackDuration;
}

const getEnemtyAttackDuration = () => {
    let enemyAttackDuration = 1;
    return enemyAttackDuration;
}

const getAttackPower = () => {
    let attackPower = 1;
    return attackPower;
}

const getAttackResistance = () => {
    let attackResistance = 1;
    return attackResistance;
}

const getInsanityThreshold = () => {
    let insanityThreshold = 1;
    return insanityThreshold;
}

const getInsanityThresholdReduction = () => {
    let insanityThresholdReduction = 1;
    return insanityThresholdReduction;
}